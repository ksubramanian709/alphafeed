[build]
buildCommand = "cd api && mvn package -q -DskipTests"

[deploy]
startCommand = "cd api && java -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -XX:+UseG1GC -XX:MaxMetaspaceSize=128m -jar target/market-feed-0.1.0.jar"
healthcheckPath = "/v1/health"
